# read user input from the console
name = input('Enter your Name: ')  # this reads the type automatically
print(type(name))  # <-- type will always be str to do math convert to ints() / floats()
print(f'{name} is your name')
